# Developer Cheatsheets 🚀

A quick reference handbook for Git workflows and command-line utilities.

## Git Essentials
- `git checkout -b <branch>`: Create and switch to new branch
- `git log --oneline --graph`: Visual compact history
- `git status`: Show modified files in workspace
- `git pull --rebase origin main`: Keep local branch synchronized
